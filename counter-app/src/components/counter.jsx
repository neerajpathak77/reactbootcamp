import React, { Component } from 'react';

class Counter extends Component {
  state = {  }
  render() { 
    // React.createElement behind the scene thats why we have to import React
    return <h1> Welcome !!</h1>;
  }
}
 
export default Counter;
